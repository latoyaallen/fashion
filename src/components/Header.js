import React from 'react';

const text = {
  paddingTop: 5,
}

const Header = (props) => (
  <div style={text}>
    <h1>
      <center>
        Peach.Haus
      </center>
    </h1>
  </div>
);

export default Header;
